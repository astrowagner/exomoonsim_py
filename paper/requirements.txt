# Python environment used to generate the Paper III figures and tables.
# Exact versions this was run and verified with (2026-07-25):
#   Python 3.10.12
numpy==2.2.6
matplotlib==3.10.9

# Minimum known-good versions (the code is pure numpy + matplotlib and has no
# other dependencies); newer point releases are expected to work:
#   numpy>=1.24
#   matplotlib>=3.7
